spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password: 
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
  h2:
    console:
      enabled: true
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: test-client-id
            client-secret: test-client-secret

server:
  port: 0

jwt:
  secret: test_jwt_secret_key_for_testing_purposes_only_not_for_production_use
  expiration: 86400000

supabase:
  url: https://test.supabase.co
  service-key: test-service-key

razorpay:
  key-id: test-key-id
  key-secret: test-key-secret

app:
  frontend-url: http://localhost:3000

logging:
  level:
    com.nerdsoncall: INFO
    org.springframework.security: INFO
